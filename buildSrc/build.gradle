apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.2.1'
    compile 'org.ow2.asm:asm:6.0'
}

repositories {
    google()
    jcenter()
}
